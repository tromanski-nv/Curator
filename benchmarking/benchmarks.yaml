# Canonical full-suite Curator benchmark configuration.
#
# This file is complete and runnable by itself. Its resource settings, timeouts,
# and performance requirements are calibrated for the default 8-GPU H100 nightly
# environment. To run the same benchmark suite on another SKU, pass this file
# first and then pass a SKU override file, for example:
#   --config benchmarking/benchmarks.yaml --config benchmarking/4xGB200-64CPU.yaml
#
# The "paths" section defines files and directories used by the benchmarks.
# Each entry requires a "name" and a "host_path". Paths must exist on the host.
# If running inside a Docker container started with tools/run.sh, each path is
# automatically mounted into the container. An optional "container_path" overrides
# the default mount point (default is host_path prefixed with "/MOUNT").
# The "name" can be used as a placeholder elsewhere in this file
# (e.g. {datasets_path}) and will be substituted with either host_path or
# container_path based on the environment.
paths:
  - name: "datasets_path"
    host_path: "/path/to/datasets"
    container_path: "/datasets"
  - name: "model_weights_path"
    host_path: "/path/to/model_weights"
    container_path: "/model_weights"
  - name: "results_path"
    host_path: "/path/where/results/are/stored"
    # container_path will default to "/MOUNT/path/where/results/are/stored".

# The following path variables are deprecated. They will still work but will produce a warning.
# Use the "paths" section above instead. These will be removed in a future release.
#
# results_path: "/path/where/results/are/stored"
# datasets_path: "/path/to/datasets"
# model_weights_path: "/path/to/model_weights"

# Background per-GPU stats recorder. While each entry runs, polls all visible GPUs
# (independent of CUDA_VISIBLE_DEVICES) at `interval_s` and writes one row per
# (timestamp, GPU) to {session_entry_path}/gpustats.csv. Set interval_s to 0 to disable.
gpu_stats_recorder:
  interval_s: 1.0

datasets:
  - name: "tinystories"
    formats:
    - type: "parquet"
      path: "{datasets_path}/tinystories/parquet_data"
    - type: "jsonl"
      path: "{datasets_path}/tinystories/jsonl_data"
  - name: "commoncrawl"
    formats:
    - type: "jsonl"
      path: "{datasets_path}/commoncrawl/jsonl_data"
    - type: "parquet_gemma_embeddings"
      path: "{datasets_path}/cleaned_exact_dedup_all_cc_gemma_embeddings"
  - name: "commoncrawl_id_map"
    formats:
    - type: "json"
      path: "{datasets_path}/commoncrawl/id_generator.json"
  - name: "commoncrawl_ids"
    formats:
    - type: "parquet"
      path: "{datasets_path}/commoncrawl/IDs/parquet_data"
  - name: "mscoco"
    formats:
    - type: "wds"
      path: "{datasets_path}/mscoco/wds/truncated_100K_mscoco_benchmarking"
  - name: "mscoco_model_weights"
    formats:
    - type: "files"
      path: "{datasets_path}/mscoco/model_weights"
  - name: "image_curation_mint1t"
    formats:
    - type: "wds"
      path: "{datasets_path}/image_curation/mint1t_unique_jpeg_1m_v1"
  - name: "videos"
    formats:
    - type: "mp4"
      path: "{datasets_path}/sample4k"
  - name: "videos_model_weights"
    formats:
    - type: "files"
      path: "{datasets_path}/video_model_weights"
  - name: "rpv2-2023-14-en"
    formats:
    - type: "parquet"
      path: "{datasets_path}/rpv2_2023-14_en"
  - name: "arxiv_downloads"
    formats:
    - type: "tar"
      path: "{datasets_path}/arxiv_downloads"
  - name: "fasttext_langid_model"
    formats:
    - type: "bin"
      path: "{model_weights_path}/fasttext/lid.176.bin"
    - type: "ftz"
      path: "{model_weights_path}/fasttext/lid.176.ftz"
  - name: "fasttext_quality_model"
    formats:
    - type: "bin"
      path: "{model_weights_path}/fasttext/model.bin"
  - name: "gretel_symptoms"
    formats:
    - type: "jsonl"
      path: "{datasets_path}/gretel_symptoms"
  - name: "text_models_hf_cache"
    formats:
    - type: "files"
      path: "{model_weights_path}/text_models_hf_cache"
  - name: "multimodal_mint1t"
    formats:
    - type: "wds_tar_dir"
      path: "{datasets_path}/multimodal/mint1t"
  - name: "finemath4plus_enriched_pdx_10x"
    formats:
    - type: "parquet"
      path: "{datasets_path}/finemath4plus/enriched_pdx_10x"
  - name: "read_speech"
    formats:
    - type: "wav"
      path: "{datasets_path}/read_speech"
  # Shared 750-hour LibriSpeech training manifest for both executors.
  - name: "librispeech_all_750h"
    formats:
    - type: "jsonl"
      path: "{datasets_path}/librispeech_all_train_750h_71cacbfb/manifest.jsonl"
  # AMI SDM meetings up to 60 minutes, totaling at least 30 hours.
  - name: "audio_tagging_ami_sdm"
    formats:
    - type: "jsonl"
      path: "{datasets_path}/audio_tagging_ami_sdm_8cdaae2_30h_max60m/manifest.jsonl"
  # AMI SDM metadata only; no audio is staged.
  - name: "alm_ami_sdm"
    formats:
    - type: "jsonl"
      path: "{datasets_path}/alm_ami_sdm_8cdaae2/manifest.jsonl"
  # 99 Common Crawl PDFs (~87MB) extracted from the CC-MAIN zip hierarchy.
  # Web-crawled rather than arXiv, so page counts and layouts vary widely,
  # which keeps the throughput numbers representative of real workloads.
  - name: "nemotron_parse_pdf"
    formats:
    - type: "manifest"
      path: "{datasets_path}/nemotron_parse_pdf/manifest.jsonl"
    - type: "pdf_dir"
      path: "{datasets_path}/nemotron_parse_pdf/pdfs"
  - name: "nemotron_parse_pdf_model"
    formats:
    - type: "files"
      path: "{model_weights_path}/nemotron_parse/NVIDIA-Nemotron-Parse-v1.2"
  # 1,800 fixed 15-minute bundles from pinned public CC-BY-4.0 LibriSpeech: 450 audio hours.
  - name: "audio_sortformer_librispeech_450h"
    formats:
    - type: "files"
      path: "{datasets_path}/audio_sortformer_librispeech_450h_1800x15m_71cacbfb"
# Timeout knobs used by the benchmark runner and by external benchmark job generators:
#   max(entry.timeout_s (or default_timeout_s) + cleanup_timeout_s, min_timeout_s)
# default_timeout_s: per-entry default; max_timeout_s: per-entry ceiling after config merging;
# cleanup_timeout_s: post-run cleanup buffer; min_timeout_s: floor covering container setup.
# max_timeout_s is 3h59m so a max-length entry plus the 60s cleanup buffer fits in 4h.
default_timeout_s: 7200
max_timeout_s: 14340
cleanup_timeout_s: 60
min_timeout_s: 600

# Optional sinks
sinks:
#  - name: mlflow
#    enabled: false
#    tracking_uri: ${MLFLOW_TRACKING_URI}
#    experiment: ray-curator-common-crawl
  - name: slack
    enabled: true
    live_updates: true
    thread_replies_for_failures_or_warnings_only: true
    channel_id: ${SLACK_CHANNEL_ID}
    default_metrics: ["exec_time_s"]
    ping_users_on_failure: true
#  - name: gdrive
#    enabled: false
#    drive_folder_id: ${GDRIVE_FOLDER_ID}
#    service_account_file: ${GDRIVE_SERVICE_ACCOUNT_FILE}

# Whether to delete scratch dirs after each run
delete_scratch: true

# The size of the object store used by Ray which can be either a value in bytes (int), or
# a fraction of total system memory (float), or the value "default" (string) which allows
# for "ray start" to determine object store size.
object_store_size: 536870912000  # 500GB

# Fraction (0.0-1.0) of GPU memory that may be in use without emitting a
# warning. Checked both before and after each benchmark run.
gpu_mem_use_warning_threshold: 0.01

# Global ray settings inherited by all entries. Per-entry ray sections override these values.
ray:
  num_cpus: 128
  num_gpus: 8
  enable_object_spilling: false

entries:
  - name: domain_classification_raydata
    enabled: true
    script: domain_classification_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
      --input-path={dataset:commoncrawl,jsonl}
      --dataset-size-gb=15
      --model-inference-batch-size=1024
    timeout_s: 1200
    sink_data:
      - name: slack
        # Additional metrics to include in the Slack report.  These must be present in the metrics.json file generated by the script.
        additional_metrics:
          - throughput_docs_per_sec
          - number_of_domains_predicted
        ping_on_failure:
          - U022P8CDX40  # Sarah Yurick

    requirements:
      - metric: throughput_docs_per_sec
        min_value: 3500
      - metric: number_of_domains_predicted
        exact_value: 26
      # We choose two domains randomly and ensure that the counts are as expected.
      - metric: domain_label_games_count
        exact_value: 94023
      - metric: domain_label_news_count
        exact_value: 149556

  - name: domain_classification_xenna
    enabled: true
    script: domain_classification_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=xenna
      --input-path={dataset:commoncrawl,jsonl}
      --dataset-size-gb=15
      --model-inference-batch-size=1024
    timeout_s: 1200
    sink_data:
      - name: slack
        additional_metrics:
          - throughput_docs_per_sec
          - number_of_domains_predicted
        ping_on_failure:
          - U022P8CDX40  # Sarah Yurick

    requirements:
      - metric: throughput_docs_per_sec
        min_value: 4000
      - metric: number_of_domains_predicted
        exact_value: 26
      # We choose two domains randomly and ensure that the counts are as expected.
      - metric: domain_label_games_count
        exact_value: 94023
      - metric: domain_label_news_count
        exact_value: 149556

  - name: embedding_generation_raydata
    enabled: true
    script: embedding_generation_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
      --input-path={dataset:commoncrawl,jsonl}
      --dataset-size-gb=25
      --model-identifier=google/embeddinggemma-300m
      --model-variation=vllm_text_pretokenized
      --cache-dir={dataset:text_models_hf_cache,files}
    timeout_s: 1200
    sink_data:
      - name: slack
        additional_metrics:
          - throughput_docs_per_sec
        ping_on_failure:
          - U022P8CDX40  # Sarah Yurick
          - U07JL5K0L10  # Praateek Mahajan

    requirements:
      - metric: throughput_docs_per_sec
        min_value: 6000

  - name: embedding_generation_xenna
    enabled: true
    script: embedding_generation_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=xenna
      --input-path={dataset:commoncrawl,jsonl}
      --dataset-size-gb=25
      --model-identifier=google/embeddinggemma-300m
      --model-variation=vllm_text_pretokenized
      --cache-dir={dataset:text_models_hf_cache,files}
    timeout_s: 1200
    sink_data:
      - name: slack
        additional_metrics:
          - num_documents_processed
          - throughput_docs_per_sec
        ping_on_failure:
          - U022P8CDX40  # Sarah Yurick
          - U07JL5K0L10  # Praateek Mahajan

    requirements:
      - metric: throughput_docs_per_sec
        min_value: 7000

  - name: exact_dedup_identification
    enabled: true
    script: exact_dedup_identification_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --input-path={dataset:rpv2-2023-14-en,parquet}
      --output-path={session_entry_dir}/scratch/output
      --input-filetype=parquet
      --text-field=raw_content
      --input-blocksize=3GiB
    timeout_s: 1200
    sink_data:
      - name: slack
        additional_metrics:
          - "num_duplicates"
        ping_on_failure:
          - UBDSVEQPL  # Ayush Dattagupta

    requirements:
      - metric: num_duplicates
        exact_value: 64292856

  - name: fuzzy_dedup_identification
    enabled: true
    script: fuzzy_dedup_identification_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --input-path={dataset:commoncrawl,jsonl}
      --cache-path={session_entry_dir}/scratch/cache
      --output-path={session_entry_dir}/scratch/output
      --input-filetype=jsonl
      --bands-per-iteration=20
      --text-field=text
      --input-blocksize=1.5GiB
    timeout_s: 1200
    sink_data:
      - name: slack
        additional_metrics:
          - num_duplicates
          - minhash_percent_time
          - lsh_percent_time
          - connected_components_percent_time
        ping_on_failure:
          - UBDSVEQPL  # Ayush Dattagupta

    requirements:
      - metric: num_duplicates
        exact_value: 59226133

  - name: minhash_file_group_task_ray_actors
    enabled: true
    script: minhash_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --input-path={dataset:rpv2-2023-14-en,parquet}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_actors
      --dataset-size-ratio=1
      --input-task-type=FileGroupTask
      --input-filetype=parquet
      --blocksize=512MiB
      --text-field=raw_content
    timeout_s: 712
    sink_data:
      - name: slack
        additional_metrics:
          - num_documents_processed
          - throughput_docs_per_sec
          - minhash_input_prep_worker_time_s_mean
          - minhash_compute_worker_time_s_mean
          - minhash_write_worker_time_s_mean
        ping_on_failure:
          - UBDSVEQPL  # Ayush Dattagupta
    requirements:
      - metric: num_documents_processed
        exact_value: 1046344809
      - metric: throughput_docs_per_sec
        min_value: 2000000
      - metric: minhash_compute_worker_time_s_mean
        min_value: 0.12
        max_value: 0.24
      # FileGroupTask input prep measures cuDF reading the source files.
      - metric: minhash_input_prep_worker_time_s_mean
        min_value: 0.22
        max_value: 0.35
      - metric: minhash_write_worker_time_s_mean
        min_value: 0.12
        max_value: 0.35

  - name: minhash_file_group_task_xenna
    enabled: false
    script: minhash_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --input-path={dataset:rpv2-2023-14-en,parquet}
      --output-path={session_entry_dir}/scratch/output
      --executor=xenna
      --dataset-size-ratio=1
      --input-task-type=FileGroupTask
      --input-filetype=parquet
      --blocksize=512MiB
      --text-field=raw_content
    timeout_s: 512
    sink_data:
      - name: slack
        additional_metrics:
          - num_documents_processed
          - throughput_docs_per_sec
          - minhash_input_prep_worker_time_s_mean
          - minhash_compute_worker_time_s_mean
          - minhash_write_worker_time_s_mean
        ping_on_failure:
          - UBDSVEQPL  # Ayush Dattagupta
    requirements:
      - metric: num_documents_processed
        exact_value: 1046344809
      - metric: throughput_docs_per_sec
        min_value: 2000000
      - metric: minhash_compute_worker_time_s_mean
        min_value: 0.12
        max_value: 0.24
      # FileGroupTask input prep measures cuDF reading the source files.
      - metric: minhash_input_prep_worker_time_s_mean
        min_value: 0.22
        max_value: 0.35
      - metric: minhash_write_worker_time_s_mean
        min_value: 0.12
        max_value: 0.35

  - name: minhash_file_group_task_ray_data
    enabled: false
    script: minhash_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --input-path={dataset:rpv2-2023-14-en,parquet}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
      --dataset-size-ratio=1
      --input-task-type=FileGroupTask
      --input-filetype=parquet
      --blocksize=512MiB
      --text-field=raw_content
    timeout_s: 512
    sink_data:
      - name: slack
        additional_metrics:
          - num_documents_processed
          - throughput_docs_per_sec
          - minhash_input_prep_worker_time_s_mean
          - minhash_compute_worker_time_s_mean
          - minhash_write_worker_time_s_mean
        ping_on_failure:
          - UBDSVEQPL  # Ayush Dattagupta
    requirements:
      - metric: num_documents_processed
        exact_value: 1046344809
      - metric: throughput_docs_per_sec
        min_value: 2000000
      - metric: minhash_compute_worker_time_s_mean
        min_value: 0.12
        max_value: 0.24
      # FileGroupTask input prep measures cuDF reading the source files.
      - metric: minhash_input_prep_worker_time_s_mean
        min_value: 0.22
        max_value: 0.35
      - metric: minhash_write_worker_time_s_mean
        min_value: 0.12
        max_value: 0.35

  - name: minhash_document_batch_xenna
    enabled: true
    script: minhash_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --input-path={dataset:rpv2-2023-14-en,parquet}
      --output-path={session_entry_dir}/scratch/output
      --executor=xenna
      --dataset-size-ratio=1
      --input-task-type=DocumentBatch
      --input-filetype=parquet
      --blocksize=512MiB
      --text-field=raw_content
    timeout_s: 820
    sink_data:
      - name: slack
        additional_metrics:
          - num_documents_processed
          - throughput_docs_per_sec
          - minhash_input_prep_worker_time_s_mean
          - minhash_compute_worker_time_s_mean
          - minhash_write_worker_time_s_mean
        ping_on_failure:
          - UBDSVEQPL  # Ayush Dattagupta
    requirements:
      - metric: num_documents_processed
        exact_value: 1046344809
      - metric: throughput_docs_per_sec
        min_value: 1600000
      - metric: minhash_compute_worker_time_s_mean
        min_value: 0.12
        max_value: 0.24
      # DocumentBatch input prep measures host-to-device transfer.
      - metric: minhash_input_prep_worker_time_s_mean
        min_value: 0.08
        max_value: 0.18
      - metric: minhash_write_worker_time_s_mean
        min_value: 0.12
        max_value: 0.35

  - name: minhash_document_batch_ray_data
    enabled: true
    script: minhash_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --input-path={dataset:rpv2-2023-14-en,parquet}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
      --dataset-size-ratio=1
      --input-task-type=DocumentBatch
      --input-filetype=parquet
      --blocksize=512MiB
      --text-field=raw_content
    timeout_s: 1400
    sink_data:
      - name: slack
        additional_metrics:
          - num_documents_processed
          - throughput_docs_per_sec
          - minhash_input_prep_worker_time_s_mean
          - minhash_compute_worker_time_s_mean
          - minhash_write_worker_time_s_mean
        ping_on_failure:
          - UBDSVEQPL  # Ayush Dattagupta
    requirements:
      - metric: num_documents_processed
        exact_value: 1046344809
      - metric: throughput_docs_per_sec
        min_value: 800000
      - metric: minhash_compute_worker_time_s_mean
        min_value: 0.12
        max_value: 0.24
      # DocumentBatch input prep measures host-to-device transfer.
      - metric: minhash_input_prep_worker_time_s_mean
        min_value: 0.08
        max_value: 0.18
      - metric: minhash_write_worker_time_s_mean
        min_value: 0.12
        max_value: 0.35

  - name: minhash_document_batch_ray_data_num_workers_8
    enabled: false
    script: minhash_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --input-path={dataset:rpv2-2023-14-en,parquet}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
      --dataset-size-ratio=1
      --input-task-type=DocumentBatch
      --input-filetype=parquet
      --blocksize=512MiB
      --text-field=raw_content
      --minhash-num-workers=8
    timeout_s: 1400
    sink_data:
      - name: slack
        additional_metrics:
          - num_documents_processed
          - throughput_docs_per_sec
          - minhash_input_prep_worker_time_s_mean
          - minhash_compute_worker_time_s_mean
          - minhash_write_worker_time_s_mean
        ping_on_failure:
          - UBDSVEQPL  # Ayush Dattagupta
    requirements:
      - metric: num_documents_processed
        exact_value: 1046344809
      - metric: throughput_docs_per_sec
        min_value: 800000
      - metric: minhash_compute_worker_time_s_mean
        min_value: 0.12
        max_value: 0.24
      # DocumentBatch input prep measures host-to-device transfer.
      - metric: minhash_input_prep_worker_time_s_mean
        min_value: 0.08
        max_value: 0.18
      - metric: minhash_write_worker_time_s_mean
        min_value: 0.12
        max_value: 0.35

  - name: semdedup_identification_xenna_dataset_size_ratio
    enabled: true
    script: semdedup_identification_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --input-path={dataset:commoncrawl,parquet_gemma_embeddings}
      --cache-path={session_entry_dir}/scratch/cache
      --output-path={session_entry_dir}/scratch/output
      --executor=xenna
      --n-clusters=1000
      --id-field=_curator_dedup_id
      --embedding-field=embeddings
      --input-filetype=parquet
      --eps=0.01
      --which-to-keep=hard
      --pairwise-batch-size=1024
      --kmeans-embedding-output-dtype=float32
      --pairwise-compute-dtype=float32
      --dataset-size-ratio=0.20
      --fit-data-fraction=1.0
    timeout_s: 1200
    sink_data:
      - name: slack
        additional_metrics:
          - workflow_total_time
          - kmeans_time
          - pairwise_time
          - num_documents_processed
          - num_duplicates
          - kmeans_footer_scan_time_s
          - kmeans_read_time_s
          - kmeans_fit_time_s
          - kmeans_predict_time_s
          - kmeans_write_time_s
          - kmeans_actual_fit_percent
          - kmeans_fit_data_fraction
          - kmeans_fit_file_fraction
          - pairwise_read_time_s
          - pairwise_conversion_time_s
          - pairwise_compute_time_s
          # Percent of KMeans/workflow wall time.
          - kmeans_read_percent_time
          - kmeans_write_percent_time
          - kmeans_fit_predict_percent_time
          - pairwise_percent_time
        ping_on_failure:
          - U07JL5K0L10  # Praateek Mahajan

    requirements:
      - metric: num_documents_processed
        exact_value: 65844782
      - metric: num_duplicates
        min_value: 1000000
        max_value: 1250000

  - name: semdedup_identification_xenna_fit_data_fraction
    enabled: true
    script: semdedup_identification_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --input-path={dataset:commoncrawl,parquet_gemma_embeddings}
      --cache-path={session_entry_dir}/scratch/cache
      --output-path={session_entry_dir}/scratch/output
      --executor=xenna
      --n-clusters=1000
      --id-field=_curator_dedup_id
      --embedding-field=embeddings
      --input-filetype=parquet
      --eps=0.01
      --which-to-keep=hard
      --pairwise-batch-size=1024
      --kmeans-embedding-output-dtype=float32
      --pairwise-compute-dtype=float32
      --fit-data-fraction=0.20
    timeout_s: 1800
    sink_data:
      - name: slack
        additional_metrics:
          - workflow_total_time
          - kmeans_time
          - pairwise_time
          - num_documents_processed
          - num_duplicates
          - kmeans_footer_scan_time_s
          - kmeans_read_time_s
          - kmeans_fit_time_s
          - kmeans_predict_time_s
          - kmeans_write_time_s
          - kmeans_actual_fit_percent
          - kmeans_fit_data_fraction
          - kmeans_fit_file_fraction
          - pairwise_read_time_s
          - pairwise_conversion_time_s
          - pairwise_compute_time_s
          # Percent of KMeans/workflow wall time.
          - kmeans_read_percent_time
          - kmeans_write_percent_time
          - kmeans_fit_predict_percent_time
          - pairwise_percent_time
        ping_on_failure:
          - U07JL5K0L10  # Praateek Mahajan

    requirements:
      - metric: num_documents_processed
        exact_value: 329774475
      - metric: num_duplicates
        min_value: 10000000
        max_value: 11000000

  - name: dedup_removal_raydata
    enabled: true
    script: dedup_removal_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --input-path={dataset:commoncrawl,jsonl}
      --id-generator-path={dataset:commoncrawl_id_map,json}
      --ids-to-remove-path={dataset:commoncrawl_ids,parquet}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
      --input-filetype=jsonl
      --output-filetype=parquet
      --id-field=_curator_dedup_id
      --duplicate-id-field=_curator_dedup_id
      --blocksize=1.5GiB
    timeout_s: 1800
    sink_data:
      - name: slack
        additional_metrics:
          - num_duplicates_removed
        ping_on_failure:
          - UBDSVEQPL  # Ayush Dattagupta
          - U07JL5K0L10  # Praateek Mahajan
    ray:
      num_gpus: 0
    requirements:
      - metric: num_duplicates_removed
        exact_value: 59226133

  - name: dedup_removal_xenna
    enabled: true
    script: dedup_removal_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --input-path={dataset:commoncrawl,jsonl}
      --id-generator-path={dataset:commoncrawl_id_map,json}
      --ids-to-remove-path={dataset:commoncrawl_ids,parquet}
      --output-path={session_entry_dir}/scratch/output
      --executor=xenna
      --input-filetype=jsonl
      --output-filetype=parquet
      --id-field=_curator_dedup_id
      --duplicate-id-field=_curator_dedup_id
      --blocksize=1.5GiB
    timeout_s: 1800
    sink_data:
      - name: slack
        additional_metrics:
          - num_duplicates_removed
          - io_percentage
        ping_on_failure:
          - UBDSVEQPL  # Ayush Dattagupta
          - U07JL5K0L10  # Praateek Mahajan
    ray:
      num_gpus: 0
    requirements:
      - metric: num_duplicates_removed
        exact_value: 59226133
      - metric: io_percentage
        min_value: 75

  - name: score_filter_raydata
    enabled: true
    script: score_filter_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
      --input-path={dataset:commoncrawl,jsonl}
      --dataset-size-gb=75
      --yaml-config={curator_repo_dir}/nemo_curator/config/text/heuristic_filter_english_pipeline.yaml
    timeout_s: 1200
    sink_data:
      - name: slack
        additional_metrics:
          - num_kept_documents
          - throughput_docs_per_sec
        ping_on_failure:
          - U022P8CDX40  # Sarah Yurick
    ray:
      num_gpus: 0
    requirements:
      # ensure the total number of documents processed is correct
      - metric: num_documents_processed
        exact_value: 17535360
      # account for stochastic filters
      - metric: num_kept_documents
        min_value: 12808500
        max_value: 12809500
      - metric: throughput_docs_per_sec
        min_value: 19000

  - name: score_filter_xenna
    enabled: true
    script: score_filter_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=xenna
      --input-path={dataset:commoncrawl,jsonl}
      --dataset-size-gb=75
      --yaml-config={curator_repo_dir}/nemo_curator/config/text/heuristic_filter_english_pipeline.yaml
    timeout_s: 1200
    sink_data:
      - name: slack
        additional_metrics:
          - num_kept_documents
          - throughput_docs_per_sec
        ping_on_failure:
          - U022P8CDX40  # Sarah Yurick
    ray:
      num_gpus: 0
    requirements:
      # ensure the total number of documents processed is correct
      - metric: num_documents_processed
        exact_value: 17535360
      # account for stochastic filters
      - metric: num_kept_documents
        min_value: 12808500
        max_value: 12809500
      - metric: throughput_docs_per_sec
        min_value: 15000

  - name: fasttext_filter_raydata
    enabled: true
    script: fasttext_filter_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
      --input-path={dataset:commoncrawl,jsonl}
      --dataset-size-gb=300
      --yaml-config={curator_repo_dir}/nemo_curator/config/text/fasttext_filter_pipeline.yaml
      --fasttext-langid-model-path={dataset:fasttext_langid_model,bin}
      --fasttext-quality-model-path={dataset:fasttext_quality_model,bin}
    timeout_s: 1200
    sink_data:
      - name: slack
        additional_metrics:
          - num_kept_documents
          - throughput_docs_per_sec
        ping_on_failure:
          - U022P8CDX40  # Sarah Yurick
    ray:
      num_gpus: 0
    requirements:
      - metric: throughput_docs_per_sec
        min_value: 60000

  - name: fasttext_filter_xenna
    enabled: true
    script: fasttext_filter_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=xenna
      --input-path={dataset:commoncrawl,jsonl}
      --dataset-size-gb=300
      --yaml-config={curator_repo_dir}/nemo_curator/config/text/fasttext_filter_pipeline.yaml
      --fasttext-langid-model-path={dataset:fasttext_langid_model,bin}
      --fasttext-quality-model-path={dataset:fasttext_quality_model,bin}
    timeout_s: 1200
    sink_data:
      - name: slack
        additional_metrics:
          - num_kept_documents
          - throughput_docs_per_sec
        ping_on_failure:
          - U022P8CDX40  # Sarah Yurick
    ray:
      num_gpus: 0
    requirements:
      - metric: throughput_docs_per_sec
        min_value: 70000

  - name: modifier_raydata
    enabled: true
    script: modifier_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
      --input-path={dataset:commoncrawl,jsonl}
      --dataset-size-gb=200
    timeout_s: 1200
    sink_data:
      - name: slack
        additional_metrics:
          - throughput_docs_per_sec
        ping_on_failure:
          - U022P8CDX40  # Sarah Yurick
    ray:
      num_gpus: 0
    requirements:
      - metric: throughput_docs_per_sec
        min_value: 55000

  - name: modifier_xenna
    enabled: true
    script: modifier_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=xenna
      --input-path={dataset:commoncrawl,jsonl}
      --dataset-size-gb=200
    timeout_s: 1200
    sink_data:
      - name: slack
        additional_metrics:
          - throughput_docs_per_sec
        ping_on_failure:
          - U022P8CDX40  # Sarah Yurick
    ray:
      num_gpus: 0
    requirements:
      - metric: throughput_docs_per_sec
        min_value: 40000

  - name: image_curation_raydata
    enabled: true
    script: image_pipeline_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --input-wds-dataset-dir={dataset:image_curation_mint1t,wds}
      --output-dataset-dir={session_entry_dir}/scratch/output
      --model-dir={dataset:mscoco_model_weights,files}
      --executor=ray_data
      --tar-files-per-partition=1
      --input-partition-limit=850
      --batch-size=1000
      --embedding-batch-size=500
      --aesthetic-batch-size=500
      --aesthetic-threshold=0.9
      --verbose
    timeout_s: 1800
    sink_data:
      - name: slack
        additional_metrics:
          - num_images_processed
          - num_images_written
          - throughput_images_per_sec
        ping_on_failure:
          - U020C9VC05N  # Ao Tang

    requirements:
      # 850 deterministic shards x 1,000 unique images per shard.
      - metric: num_images_processed
        exact_value: 850000
      - metric: throughput_images_per_sec
        min_value: 350.0

  - name: image_curation_xenna
    enabled: true
    script: image_pipeline_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --input-wds-dataset-dir={dataset:image_curation_mint1t,wds}
      --output-dataset-dir={session_entry_dir}/scratch/output
      --model-dir={dataset:mscoco_model_weights,files}
      --executor=xenna
      --tar-files-per-partition=1
      --input-partition-limit=850
      --batch-size=1000
      --embedding-batch-size=500
      --aesthetic-batch-size=500
      --aesthetic-threshold=0.9
      --verbose
    timeout_s: 1500
    sink_data:
      - name: slack
        additional_metrics:
          - num_images_processed
          - num_images_written
          - throughput_images_per_sec
        ping_on_failure:
          - U020C9VC05N  # Ao Tang

    requirements:
      # 850 deterministic shards x 1,000 unique images per shard.
      - metric: num_images_processed
        exact_value: 850000
      - metric: throughput_images_per_sec
        min_value: 900.0

  # Both backends process the same public cohort with stage-owned defaults.
  - name: audio_sortformer_xenna
    enabled: true
    script: audio_sortformer_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --scratch-output-path={session_entry_dir}/scratch
      --raw-data-dir={dataset:audio_sortformer_librispeech_450h,files}
      --model-path={model_weights_path}/audio_sortformer/diar_streaming_sortformer_4spk-v2.1.nemo
      --executor=xenna
      --rttm-out-dir={session_entry_dir}/scratch/rttm
    timeout_s: 1800
    sink_data:
      - name: slack
        additional_metrics:
          - num_segments_processed
          - throughput_audio_hours_per_hour
        ping_on_failure:
          - U03C41SNADV  # Aaftab V
    requirements:
      - metric: num_input_rows
        exact_value: 1800
      - metric: num_output_rows
        exact_value: 1800
      - metric: num_tasks_with_segments
        exact_value: 1800
      - metric: total_audio_duration_hours
        exact_value: 450.0
      - metric: throughput_audio_hours_per_hour
        min_value: 1.0

  - name: audio_sortformer_raydata
    enabled: true
    script: audio_sortformer_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --scratch-output-path={session_entry_dir}/scratch
      --raw-data-dir={dataset:audio_sortformer_librispeech_450h,files}
      --model-path={model_weights_path}/audio_sortformer/diar_streaming_sortformer_4spk-v2.1.nemo
      --executor=ray_data
      --rttm-out-dir={session_entry_dir}/scratch/rttm
    timeout_s: 1800
    sink_data:
      - name: slack
        additional_metrics:
          - num_segments_processed
          - throughput_audio_hours_per_hour
        ping_on_failure:
          - U03C41SNADV  # Aaftab V
    requirements:
      - metric: num_input_rows
        exact_value: 1800
      - metric: num_output_rows
        exact_value: 1800
      - metric: num_tasks_with_segments
        exact_value: 1800
      - metric: total_audio_duration_hours
        exact_value: 450.0
      - metric: throughput_audio_hours_per_hour
        min_value: 1.0

  # Shared 750-hour workload; the executor selects workers.
  - name: audio_librispeech_xenna
    enabled: true
    script: audio_librispeech_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --input-manifest={dataset:librispeech_all_750h,jsonl}
      --model-name=nvidia/parakeet-tdt-0.6b-v2
      --wer-threshold=1000
      --executor=xenna
      --execution-mode=streaming
    timeout_s: 13740
    sink_data:
      - name: slack
        additional_metrics:
          - throughput_tasks_per_sec
          - num_tasks_processed
        ping_on_failure:
          - U03C41SNADV  # Aaftab V
    requirements:
      - metric: num_tasks_processed
        min_value: 1
      - metric: total_audio_duration_hours
        min_value: 750.0
        max_value: 750.02
      - metric: wer_output_coverage_ratio
        exact_value: 1.0

  # Same real input as Xenna.
  - name: audio_librispeech_raydata
    enabled: true
    script: audio_librispeech_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --input-manifest={dataset:librispeech_all_750h,jsonl}
      --model-name=nvidia/parakeet-tdt-0.6b-v2
      --wer-threshold=1000
      --executor=ray_data
    timeout_s: 13740
    sink_data:
      - name: slack
        additional_metrics:
          - throughput_tasks_per_sec
          - num_tasks_processed
        ping_on_failure:
          - U03C41SNADV  # Aaftab V
    requirements:
      - metric: num_tasks_processed
        min_value: 1
      - metric: total_audio_duration_hours
        min_value: 750.0
        max_value: 750.02
      - metric: wer_output_coverage_ratio
        exact_value: 1.0

  - name: audio_readspeech_xenna
    enabled: true
    script: audio_readspeech_benchmark.py
    # Hang guard only (3h30m). Full dataset with all filters runs ~3.24h on 4×A100.
    # Performance regressions should be tracked via a `requirements` check on
    # `time_taken_s` once a stable baseline is established (follow-up).
    timeout_s: 12600
    args: >-
      --benchmark-results-path={session_entry_dir}
      --scratch-output-path={session_entry_dir}/scratch
      --raw-data-dir={dataset:read_speech,wav}
      --no-auto-download
      --executor=xenna
      --max-samples=-1
      --sample-rate=48000
      --enable-vad
      --enable-sigmos
      --enable-utmos
      --enable-band-filter
      --enable-speaker-separation
    sink_data:
      - name: slack
        additional_metrics:
          - throughput_segments_per_sec
          - num_output_segments
          - time_taken_s
        ping_on_failure:
          - U07EVDG9FBK  # Shubham Bhawsar

  - name: audio_readspeech_raydata
    enabled: true
    script: audio_readspeech_benchmark.py
    # Hang guard only (3h30m). Mirrors xenna config so the two executors are
    # directly comparable on the same workload (full dataset, all filters).
    timeout_s: 12600
    args: >-
      --benchmark-results-path={session_entry_dir}
      --scratch-output-path={session_entry_dir}/scratch
      --raw-data-dir={dataset:read_speech,wav}
      --no-auto-download
      --executor=ray_data
      --max-samples=-1
      --sample-rate=48000
      --enable-vad
      --enable-sigmos
      --enable-utmos
      --enable-band-filter
      --enable-speaker-separation
    sink_data:
      - name: slack
        additional_metrics:
          - throughput_segments_per_sec
          - num_output_segments
          - time_taken_s
        ping_on_failure:
          - U07EVDG9FBK  # Shubham Bhawsar

  - name: arxiv_e2e_pipeline_raydata
    enabled: true
    script: arxiv_e2e_pipeline_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --tar-input-path={dataset:arxiv_downloads,tar}
      --output-path={session_entry_dir}/scratch/output
      --fasttext-langid-model-path={dataset:fasttext_langid_model,bin}
      --executor=ray_data
    timeout_s: 1200
    sink_data:
      - name: slack
        additional_metrics:
          - throughput_docs_per_sec
          - num_input_documents
          - num_output_documents
        ping_on_failure:
          - UGFRT88RE  # Vibhu Jawa
    ray:
      num_cpus: 16
    requirements:
      # Data integrity checks
      - metric: num_tar_files
        exact_value: 100
      - metric: num_input_documents
        exact_value: 173247
      - metric: num_output_documents
        exact_value: 116538

  - name: arxiv_e2e_pipeline_xenna
    enabled: true
    script: arxiv_e2e_pipeline_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --tar-input-path={dataset:arxiv_downloads,tar}
      --output-path={session_entry_dir}/scratch/output
      --fasttext-langid-model-path={dataset:fasttext_langid_model,bin}
      --executor=xenna
    timeout_s: 1200
    sink_data:
      - name: slack
        additional_metrics:
          - throughput_docs_per_sec
          - num_input_documents
          - num_output_documents
        ping_on_failure:
          - UGFRT88RE  # Vibhu Jawa
    ray:
      num_cpus: 16
    requirements:
      # Data integrity checks
      - metric: num_tar_files
        exact_value: 100
      - metric: num_input_documents
        exact_value: 173247
      - metric: num_output_documents
        exact_value: 116538

  - name: video_embedding_xenna
    enabled: true
    script: video_pipeline_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=xenna
      --video-dir={dataset:videos,mp4}
      --model-dir={dataset:videos_model_weights,files}
      --video-limit=4000
      --transcode-encoder=libopenh264
    timeout_s: 1800
    sink_data:
      - name: slack
        ping_on_failure:
          - U082G3E46R0  # Abhinav Garg

    requirements:
      # ensure the total number of documents processed is correct
      - metric: num_clips_generated
        exact_value: 7600
      - metric: throughput_clips_per_sec
        min_value: 12.0

  - name: video_embedding_raydata
    enabled: true
    script: video_pipeline_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
      --video-dir={dataset:videos,mp4}
      --model-dir={dataset:videos_model_weights,files}
      --video-limit=4000
      --transcode-encoder=libopenh264
    timeout_s: 2000
    sink_data:
      - name: slack
        ping_on_failure:
          - U082G3E46R0  # Abhinav Garg

    requirements:
      # ensure the total number of documents processed is correct
      - metric: num_clips_generated
        exact_value: 7600
      - metric: throughput_clips_per_sec
        min_value: 12.0

  - name: video_transcoding_xenna
    enabled: true
    script: video_pipeline_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=xenna
      --no-generate-embeddings
      --video-dir={dataset:videos,mp4}
      --video-limit=4000
      --transcode-encoder=libopenh264
    timeout_s: 1800
    sink_data:
      - name: slack
        ping_on_failure:
          - U082G3E46R0  # Abhinav Garg
    ray:
      num_gpus: 0
    requirements:
      # ensure the total number of documents processed is correct
      - metric: num_clips_generated
        exact_value: 7600
      - metric: throughput_clips_per_sec
        min_value: 15.5

  - name: video_transcoding_raydata
    enabled: true
    script: video_pipeline_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
      --no-generate-embeddings
      --video-dir={dataset:videos,mp4}
      --video-limit=4000
      --transcode-encoder=libopenh264
    timeout_s: 1800
    sink_data:
      - name: slack
        ping_on_failure:
          - U082G3E46R0  # Abhinav Garg
    ray:
      num_gpus: 0
    requirements:
      # ensure the total number of documents processed is correct
      - metric: num_clips_generated
        exact_value: 7600
      - metric: throughput_clips_per_sec
        min_value: 17.5

  - name: video_captioning_xenna
    enabled: true
    script: video_pipeline_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=xenna
      --video-dir={dataset:videos,mp4}
      --model-dir={dataset:videos_model_weights,files}
      --generate-captions
      --no-generate-embeddings
      --enhance-captions
      --video-limit=512
      --transcode-encoder=libopenh264
    timeout_s: 1800
    sink_data:
      - name: slack
        ping_on_failure:
          - U082G3E46R0  # Abhinav Garg

    requirements:
      # ensure the total number of documents processed is correct
      - metric: num_clips_generated
        exact_value: 1013
      - metric: throughput_clips_per_sec
        min_value: 0.85

  - name: video_captioning_raydata
    enabled: true
    script: video_pipeline_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
      --video-dir={dataset:videos,mp4}
      --model-dir={dataset:videos_model_weights,files}
      --generate-captions
      --no-generate-embeddings
      --enhance-captions
      --video-limit=512
      --transcode-encoder=libopenh264
    timeout_s: 1800
    sink_data:
      - name: slack
        ping_on_failure:
          - U082G3E46R0  # Abhinav Garg

    requirements:
      # ensure the total number of documents processed is correct
      - metric: num_clips_generated
        exact_value: 1013
      - metric: throughput_clips_per_sec
        min_value: 0.7

  - name: ndd_dynamo
    enabled: true
    script: ndd_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --input-path={dataset:commoncrawl,jsonl}
      --dataset-size-gb=1
      --output-path={session_entry_dir}/scratch/output
      --inference-server-type=dynamo
      --model-id=openai/gpt-oss-20b
      --model-path={model_weights_path}/text_model_weights/hf_cache/models--openai--gpt-oss-20b/snapshots/6cee5e81ee83917806bbde320786a8fb61efebee
      --executor=ray_data
      --engine-kwargs='{"tensor_parallel_size": 1}'
      --autoscaling-config='{"min_replicas": 8, "max_replicas": 8}'
    timeout_s: 2100
    ray:
      num_cpus: 32
    sink_data:
      - name: slack
        additional_metrics:
          - input_row_count
          - output_row_count
          - input_tokens_median_per_record
          - output_tokens_median_per_record
          - throughput_rows_per_sec
          - serve_startup_s
        ping_on_failure:
          - U07JL5K0L10  # Praateek Mahajan
          - U022P8CDX40  # Sarah Yurick
    requirements:
      - metric: output_row_count
        min_value: 245400
        max_value: 245500
      - metric: throughput_rows_per_sec
        min_value: 150

  - name: ndd_ray_serve
    enabled: true
    script: ndd_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --input-path={dataset:commoncrawl,jsonl}
      --dataset-size-gb=1
      --output-path={session_entry_dir}/scratch/output
      --inference-server-type=ray-serve
      --model-id=openai/gpt-oss-20b
      --model-path={model_weights_path}/text_model_weights/hf_cache/models--openai--gpt-oss-20b/snapshots/6cee5e81ee83917806bbde320786a8fb61efebee
      --executor=ray_data
      --engine-kwargs='{"tensor_parallel_size": 1, "max_model_len": 131072}'
      --autoscaling-config='{"min_replicas": 8, "max_replicas": 8}'
    timeout_s: 2100
    ray:
      num_cpus: 32
    sink_data:
      - name: slack
        additional_metrics:
          - input_row_count
          - output_row_count
          - input_tokens_median_per_record
          - output_tokens_median_per_record
          - throughput_rows_per_sec
          - serve_startup_s
        ping_on_failure:
          - U07JL5K0L10  # Praateek Mahajan
          - U022P8CDX40  # Sarah Yurick
    requirements:
      - metric: output_row_count
        min_value: 245400
        max_value: 245500
      - metric: throughput_rows_per_sec
        min_value: 150

  - name: video_transnetv2_motion_aesthetic_filter_embeddings_xenna
    enabled: true
    script: video_pipeline_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=xenna
      --video-dir={dataset:videos,mp4}
      --model-dir={dataset:videos_model_weights,files}
      --splitting-algorithm=transnetv2
      --motion-filter=enable
      --aesthetic-threshold=3.5
      --transnetv2-frame-decoder-mode ffmpeg_cpu
      --video-limit=4000
      --transcode-encoder=libopenh264
    timeout_s: 1800
    sink_data:
      - name: slack
        ping_on_failure:
          - U082G3E46R0  # Abhinav Garg

    requirements:
      # ensure the total number of documents processed is correct
      - metric: num_clips_generated
        exact_value: 526
      - metric: throughput_clips_per_sec
        min_value: 0.75

  - name: video_transnetv2_motion_aesthetic_filter_embeddings_raydata
    enabled: true
    script: video_pipeline_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
      --video-dir={dataset:videos,mp4}
      --model-dir={dataset:videos_model_weights,files}
      --splitting-algorithm=transnetv2
      --motion-filter=enable
      --aesthetic-threshold=3.5
      --transnetv2-frame-decoder-mode ffmpeg_cpu
      --video-limit=4000
      --transcode-encoder=libopenh264
    timeout_s: 2400
    sink_data:
      - name: slack
        ping_on_failure:
          - U082G3E46R0  # Abhinav Garg

    requirements:
      # ensure the total number of documents processed is correct
      - metric: num_clips_generated
        exact_value: 526
      - metric: throughput_clips_per_sec
        min_value: 0.65

  # 56 AMI meetings (30.2032h).
  - name: audio_tagging_tts_xenna
    enabled: true
    script: audio_tagging_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --scratch-output-path={session_entry_dir}/scratch
      --input-manifest={dataset:audio_tagging_ami_sdm,jsonl}
      --no-auto-download
      --diarization-model-path={model_weights_path}/audio_tagging/pyannote-speaker-diarization-community-1_8a52737
      --max-segment-length=40
      --asr-batch-size=1
      --squim-compute-batch-size=8
      --executor=xenna
    timeout_s: 3600
    sink_data:
      - name: slack
        additional_metrics:
          - num_segments_processed
          - throughput_audio_hours_per_hour
        ping_on_failure:
          - U06J49AK7BQ  # Sushmitha Deva
    requirements:
      - metric: num_input_rows
        exact_value: 56
      - metric: segment_task_coverage_ratio
        exact_value: 1.0
      - metric: num_segments_processed
        min_value: 100
      - metric: segment_output_coverage_ratio
        min_value: 0.7
      - metric: total_audio_duration_hours
        min_value: 30
        max_value: 31
      - metric: tagged_audio_duration_hours
        min_value: 0.2
      - metric: throughput_audio_hours_per_hour
        min_value: 1.0

  - name: audio_tagging_tts_raydata
    enabled: true
    script: audio_tagging_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --scratch-output-path={session_entry_dir}/scratch
      --input-manifest={dataset:audio_tagging_ami_sdm,jsonl}
      --no-auto-download
      --diarization-model-path={model_weights_path}/audio_tagging/pyannote-speaker-diarization-community-1_8a52737
      --max-segment-length=40
      --asr-batch-size=1
      --squim-compute-batch-size=8
      --executor=ray_data
    timeout_s: 3600
    sink_data:
      - name: slack
        additional_metrics:
          - num_segments_processed
          - throughput_audio_hours_per_hour
        ping_on_failure:
          - U06J49AK7BQ  # Sushmitha Deva
    requirements:
      - metric: num_input_rows
        exact_value: 56
      - metric: segment_task_coverage_ratio
        exact_value: 1.0
      - metric: num_segments_processed
        min_value: 100
      - metric: segment_output_coverage_ratio
        min_value: 0.7
      - metric: total_audio_duration_hours
        min_value: 30
        max_value: 31
      - metric: tagged_audio_duration_hours
        min_value: 0.2
      - metric: throughput_audio_hours_per_hour
        min_value: 1.0

  - name: interleaved_filter_raydata_materialize
    enabled: true
    script: interleaved_filter_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --executor=ray_data
      --input-path={dataset:multimodal_mint1t,wds_tar_dir}
      --output-path={session_entry_dir}/scratch/output
      --clip-model-dir={dataset:mscoco_model_weights,files}
      --num-files=1000
      --files-per-partition=2
      --mode=overwrite
      --materialize-on-write
      --output-max-batch-bytes=64000000
    timeout_s: 1800
    sink_data:
      - name: slack
        additional_metrics:
          - throughput_rows_per_sec
          - num_rows
          - num_output_files
          - materialize_error_count
        ping_on_failure:
          - UGFRT88RE
    requirements:
      # Allow +/-0.01% around the observed 318,062 images and 716,038 rows.
      - metric: output_num_images
        min_value: 318031
        max_value: 318093
      - metric: output_num_rows
        min_value: 715967
        max_value: 716109


  - name: interleaved_filter_xenna_materialize
    enabled: true
    script: interleaved_filter_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --executor=xenna
      --input-path={dataset:multimodal_mint1t,wds_tar_dir}
      --output-path={session_entry_dir}/scratch/output
      --clip-model-dir={dataset:mscoco_model_weights,files}
      --num-files=1000
      --files-per-partition=2
      --mode=overwrite
      --materialize-on-write
      --output-max-batch-bytes=64000000
    timeout_s: 1800
    sink_data:
      - name: slack
        additional_metrics:
          - throughput_rows_per_sec
          - num_rows
          - num_output_files
          - materialize_error_count
        ping_on_failure:
          - UGFRT88RE
    requirements:
      # Allow +/-0.01% around the observed 318,062 images and 716,038 rows.
      - metric: output_num_images
        min_value: 318031
        max_value: 318093
      - metric: output_num_rows
        min_value: 715967
        max_value: 716109


  - name: nemotron_parse_pdf_xenna
    enabled: true
    script: nemotron_parse_pdf_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --executor=xenna
      --manifest={dataset:nemotron_parse_pdf,manifest}
      --pdf-dir={dataset:nemotron_parse_pdf,pdf_dir}
      --output-dir={session_entry_dir}/scratch/output
      --model-path={dataset:nemotron_parse_pdf_model,files}
      --backend=vllm
      --enforce-eager
      --pdfs-per-task=20
      --max-pdfs=1278
      --max-pages=10
      --max-num-seqs=64
    timeout_s: 1200
    requirements:
      # Exact counts: the dataset is fixed and every file in it renders, so a
      # correct run reproduces these precisely. Verified identical across three
      # runs on both executors. Any drift means PDFs are being dropped, which is
      # the failure this entry exists to catch.
      - metric: num_pdfs_processed
        exact_value: 1278
      - metric: num_pages_processed
        exact_value: 5794
      # Throughput floors rather than exact values: wall-clock moves with cluster
      # load. Observed 8.5-8.9 pages/s and 7166-7482 output tokens/s across both
      # executors, so these sit ~15-18% below the slowest measurement -- tight
      # enough to catch a real regression, loose enough not to flake.
      # The token rate is asserted as well as the page rate so that swapping the
      # inference backend (e.g. to Dynamo) has to preserve generation throughput,
      # not just page throughput. It is a rate, not a count: exact token counts
      # are not reproducible under dynamic batching.
      - metric: throughput_pages_per_sec
        min_value: 7.0
      - metric: throughput_output_tokens_per_sec
        min_value: 6000
      # Total generated tokens, asserted as a band rather than an exact value.
      # Dynamic batching shifts where the model emits EOS, so the count is not
      # reproducible the way the page count is -- but it is stable to within 1%.
      # Measured across 10 runs (both executors): 4,852,596 to 4,897,124, mean
      # 4,872,307, total spread 0.91%. The band is +/-5% of that mean, ~10x the
      # observed drift, so it will not flake but will still catch a change in
      # what the model generates -- e.g. swapping the inference backend for one
      # that is materially more or less verbose, which a throughput floor alone
      # would miss.
      - metric: num_output_tokens
        min_value: 4630000
        max_value: 5115000

  - name: nemotron_parse_pdf_raydata
    enabled: true
    script: nemotron_parse_pdf_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --executor=ray_data
      --manifest={dataset:nemotron_parse_pdf,manifest}
      --pdf-dir={dataset:nemotron_parse_pdf,pdf_dir}
      --output-dir={session_entry_dir}/scratch/output
      --model-path={dataset:nemotron_parse_pdf_model,files}
      --backend=vllm
      --enforce-eager
      --pdfs-per-task=20
      --max-pdfs=1278
      --max-pages=10
      --max-num-seqs=64
    timeout_s: 1200
    requirements:
      # Exact counts: the dataset is fixed and every file in it renders, so a
      # correct run reproduces these precisely. Verified identical across three
      # runs on both executors. Any drift means PDFs are being dropped, which is
      # the failure this entry exists to catch.
      - metric: num_pdfs_processed
        exact_value: 1278
      - metric: num_pages_processed
        exact_value: 5794
      # Throughput floors rather than exact values: wall-clock moves with cluster
      # load. Observed 8.5-8.9 pages/s and 7166-7482 output tokens/s across both
      # executors, so these sit ~15-18% below the slowest measurement -- tight
      # enough to catch a real regression, loose enough not to flake.
      # The token rate is asserted as well as the page rate so that swapping the
      # inference backend (e.g. to Dynamo) has to preserve generation throughput,
      # not just page throughput. It is a rate, not a count: exact token counts
      # are not reproducible under dynamic batching.
      - metric: throughput_pages_per_sec
        min_value: 7.0
      - metric: throughput_output_tokens_per_sec
        min_value: 6000
      # Total generated tokens, asserted as a band rather than an exact value.
      # Dynamic batching shifts where the model emits EOS, so the count is not
      # reproducible the way the page count is -- but it is stable to within 1%.
      # Measured across 10 runs (both executors): 4,852,596 to 4,897,124, mean
      # 4,872,307, total spread 0.91%. The band is +/-5% of that mean, ~10x the
      # observed drift, so it will not flake but will still catch a change in
      # what the model generates -- e.g. swapping the inference backend for one
      # that is materially more or less verbose, which a throughput floor alone
      # would miss.
      - metric: num_output_tokens
        min_value: 4630000
        max_value: 5115000

  - name: nemotron_parse_pdf_inference_server_ray_serve
    enabled: true
    script: nemotron_parse_pdf_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --executor=ray_data
      --manifest={dataset:nemotron_parse_pdf,manifest}
      --pdf-dir={dataset:nemotron_parse_pdf,pdf_dir}
      --output-dir={session_entry_dir}/scratch/output
      --model-path={dataset:nemotron_parse_pdf_model,files}
      --model-id=nvidia/NVIDIA-Nemotron-Parse-v1.2
      --backend=vllm
      --inference-server-type=ray-serve
      --inference-server-client-workers-per-replica=4
      --inference-batch-size=32
      --enforce-eager
      --pdfs-per-task=20
      --max-pdfs=1278
      --max-pages=10
    timeout_s: 1800
    requirements:
      - metric: num_pdfs_processed
        exact_value: 1278
      - metric: num_pages_processed
        exact_value: 5794
      - metric: throughput_pages_per_sec
        min_value: 7.0
      - metric: throughput_output_tokens_per_sec
        min_value: 6000
      - metric: num_output_tokens
        min_value: 4630000
        max_value: 5115000

  - name: nemotron_parse_pdf_inference_server_dynamo
    enabled: true
    script: nemotron_parse_pdf_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --executor=ray_data
      --manifest={dataset:nemotron_parse_pdf,manifest}
      --pdf-dir={dataset:nemotron_parse_pdf,pdf_dir}
      --output-dir={session_entry_dir}/scratch/output
      --model-path={dataset:nemotron_parse_pdf_model,files}
      --model-id=nvidia/NVIDIA-Nemotron-Parse-v1.2
      --backend=vllm
      --inference-server-type=dynamo
      --inference-server-client-workers-per-replica=4
      --inference-batch-size=32
      --enforce-eager
      --pdfs-per-task=20
      --max-pdfs=1278
      --max-pages=10
    timeout_s: 1800
    requirements:
      - metric: num_pdfs_processed
        exact_value: 1278
      - metric: num_pages_processed
        exact_value: 5794
      - metric: throughput_pages_per_sec
        min_value: 7.0
      - metric: throughput_output_tokens_per_sec
        min_value: 6000
      - metric: num_output_tokens
        min_value: 4630000
        max_value: 5115000
  # CPU-only; the 8-GPU runtime target does not apply.
  - name: alm_pipeline_xenna
    enabled: true
    script: alm_pipeline_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --input-manifest={dataset:alm_ami_sdm,jsonl}
      --executor=xenna
      --target-window-duration=120.0
      --tolerance=0.1
      --min-sample-rate=16000
      --min-bandwidth=0
      --min-speakers=2
      --max-speakers=5
      --overlap-percentage=50
      --no-truncation
    timeout_s: 600
    sink_data:
      - name: slack
        additional_metrics:
          - throughput_entries_per_sec
          - throughput_windows_per_sec
        ping_on_failure:
          - U03C41SNADV  # Aaftab V
    ray:
      num_cpus: 8
      num_gpus: 0
    requirements:
      - metric: num_output_entries
        exact_value: 168
      - metric: total_builder_windows
        min_value: 1
      - metric: total_filtered_windows
        min_value: 1

  - name: alm_pipeline_ray_data
    enabled: true
    script: alm_pipeline_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --input-manifest={dataset:alm_ami_sdm,jsonl}
      --executor=ray_data
      --target-window-duration=120.0
      --tolerance=0.1
      --min-sample-rate=16000
      --min-bandwidth=0
      --min-speakers=2
      --max-speakers=5
      --overlap-percentage=50
      --no-truncation
    timeout_s: 600
    sink_data:
      - name: slack
        additional_metrics:
          - throughput_entries_per_sec
          - throughput_windows_per_sec
        ping_on_failure:
          - U03C41SNADV  # Aaftab V
    ray:
      num_cpus: 8
      num_gpus: 0
    requirements:
      - metric: num_output_entries
        exact_value: 168
      - metric: total_builder_windows
        min_value: 1
      - metric: total_filtered_windows
        min_value: 1

  # Keep this executor comparison focused on the streaming-only pipeline:
  # ParquetReader -> MathExtractStage -> JsonlWriter.
  - name: math_preprocess_raydata
    enabled: true
    script: math_pipeline_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --input-path={dataset:finemath4plus_enriched_pdx_10x,parquet}
      --output-path={session_entry_dir}/scratch/output
      --executor=ray_data
    timeout_s: 3600
    sink_data:
      - name: slack
        additional_metrics:
          - throughput_docs_per_sec
        ping_on_failure:
          - UGFRT88RE  # Vibhu Jawa
          - U6SH64WDC  # Hongsheng Fang
    ray:
      num_gpus: 0
    requirements:
      - metric: num_input_documents
        exact_value: 5233987
      - metric: num_output_documents
        exact_value: 5233987

  - name: math_preprocess_xenna
    enabled: true
    script: math_pipeline_benchmark.py
    args: >-
      --benchmark-results-path={session_entry_dir}
      --input-path={dataset:finemath4plus_enriched_pdx_10x,parquet}
      --output-path={session_entry_dir}/scratch/output
      --executor=xenna
    timeout_s: 7200
    sink_data:
      - name: slack
        additional_metrics:
          - throughput_docs_per_sec
        ping_on_failure:
          - UGFRT88RE  # Vibhu Jawa
          - U6SH64WDC  # Hongsheng Fang
    ray:
      num_gpus: 0
    requirements:
      - metric: num_input_documents
        exact_value: 5233987
      - metric: num_output_documents
        exact_value: 5233987
